# Core requirement (Python standard library only, no external packages needed)
# If you add bot or API support later, you can include:
 requests==2.31.0         # For HTTP requests
 python-telegram-bot==20.6  # If using Telegram integration
 numpy==1.26.4            # For potential ML or sentence scoring
 pandas==2.2.2            # For organizing data pools or logging
 openai==1.30.0           # If you connect with GPT APIs for dynamic replies
